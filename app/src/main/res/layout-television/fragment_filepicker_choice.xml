<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="600dp"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center"
    android:layout_gravity="center"
    >

    <!-- Title -->
    <TextView
        android:id="@android:id/message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="34dp"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        android:textStyle="bold"
        android:fontFamily="@font/inter_medium"
        android:textSize="18dp"
        android:textColor="@android:color/white"
        tools:text="Open Media" />

    <!-- Pick file -->
    <Button
        android:id="@+id/fileBtn"
        style="@style/GlassButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:paddingVertical="14dp"
        android:elevation="8dp"
        android:drawablePadding="12dp"
        android:text="@string/action_pick_file_old"
        android:textAllCaps="false"
        android:nextFocusDown="@id/urlBtn"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        />

    <!-- Open URL -->
    <Button
        android:id="@+id/urlBtn"
        style="@style/GlassButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:elevation="8dp"
        android:paddingVertical="14dp"
        android:drawablePadding="12dp"
        android:text="@string/action_open_url"
        android:textAllCaps="false"
        android:nextFocusUp="@id/fileBtn"
        android:nextFocusDown="@id/docBtn"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        />

    <!-- Open document -->
    <Button
        android:id="@+id/docBtn"
        style="@style/GlassButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="14dp"
        android:elevation="8dp"
        android:drawablePadding="12dp"
        android:text="@string/action_open_doc"
        android:textAllCaps="false"
        android:nextFocusUp="@id/urlBtn"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        />

</LinearLayout>
